# verilog-keylock-fsm
A Verilog-based digital keypad lock implemented using a finite state machine (FSM). Features password verification, failed-attempt tracking, automatic keypad lockout after three incorrect attempts, timed unlock, and a comprehensive testbench with simulation waveforms.
